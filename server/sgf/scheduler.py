"""
    Handle scheduling for smart controllers
    Uses cron-style scheduling, but with second resolution
    instead of minute.
"""

class Cron(object):
    def __init__(self):

    def add(self, job, **clockargs):
        if 'second' in clockargs:
        else:
        if 'minute' in clockargs:
        if 'hour' in clockargs:
        if 'day' in clockargs:
        if 'month' in clockargs:
        if 'year' in clockargs:
        if 'dow' in clockargs:
# vim:ai sw=4 expandtab:
